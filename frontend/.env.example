# URL de l'API en production (build)
# En développement, le proxy Vite redirige /api → http://localhost:3000
# Pour builder vers Render :
#   VITE_API_URL=https://banking-api-rdlr.onrender.com npm run build
VITE_API_URL=
